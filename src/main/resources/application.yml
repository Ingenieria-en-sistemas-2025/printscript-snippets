server:
  port: ${SERVER_PORT:8080}

spring:
  profiles:
    active: dev
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/snippets}
    username: ${SPRING_DATASOURCE_USERNAME:snippets}
    password: ${SPRING_DATASOURCE_PASSWORD:snippets}
  jpa:
    hibernate:
      ddl-auto: validate
      properties:
        # Para mapear JSON a jsonb en Postgres con Hibernate 6
        hibernate.type.preferred_json_sql_type: JSONB
        # Evita líos de huso horario (opcional)
        hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

execution:
  base-url: ${EXECUTION_BASE_URL:http://localhost:8080}

